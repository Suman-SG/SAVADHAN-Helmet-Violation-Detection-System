The `database/traffic.db` file contains local SQLite data (registered vehicles, violations, emails). It has been removed from the repository for privacy and replaced with this placeholder.

Keep a local backup if you need to preserve data; do not commit the DB to Git.
